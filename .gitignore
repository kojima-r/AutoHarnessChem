__pycache__/
*.pyc
*.egg-info/
.venv/
build/
dist/

# downloaded models (e.g. `ln -s /path/to/aizynth_data data/aizynth`)
data/

# demo output (regenerated by `ahc demo` / examples/run_examples.sh)
examples/output/

# runtime output (regenerated per run)
workspaces/*
!workspaces/.gitkeep
traces/*
!traces/.gitkeep

# node（任意: `npm install --no-save jsdom` で構造描画テストを有効化するときだけ作られる）
node_modules/
package-lock.json

# compiled skill deployments (regenerated by `ahc skills compile`)
.claude/skills/
build/skills/

# evolver output (proposals are reviewed as git diffs, not committed automatically)
evolver/proposals/

# secrets
.env

# ChemEval: ダウンロードしたデータセットと評価結果（再生成できる）
benchmarks_chemeval/data/
benchmarks_chemeval/results/
# ChemEval 公式リポジトリのクローン（独自 .git を持つ入れ子。親にはコミットしない）
benchmarks_chemeval/ChemEval/

# ChemBench: ダウンロードしたデータセットと評価結果（再生成できる）
benchmarks_chembench/data/
benchmarks_chembench/results/
# ChemBench 公式リポジトリのクローン（独自 .git を持つ入れ子。親にはコミットしない）
benchmarks_chembench/chembench/
# まれにエージェントが答えファイルを workspace ではなくプロセスの cwd（= リポジトリ
# 直下）に書く。採点には影響しない（抽出は workspace 以下しか見ない）が、全件 run は
# 数日かかるので git status を汚さないよう無視する。詳細は benchmarks_chembench/README.md
/chembench_answer.json
